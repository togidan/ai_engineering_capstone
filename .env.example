# OpenAI API Configuration (optional)
# If not provided, the system will fall back to regex-based parsing and deterministic draft generation
OPENAI_API_KEY=your_openai_api_key_here

# Milvus Vector Database Configuration
MILVUS_URI=your_milvus_uri_here
MILVUS_TOKEN=your_milvus_token_here
MILVUS_DB=capstone
MILVUS_COLLECTION=kb_chunks

# Database Configuration
# For PostgreSQL (production): Use full connection string from Supabase
# DATABASE_URL=postgresql://user:password@host:port/database
# For SQLite (development): Leave empty to use local SQLite file
DATABASE_URL=

# Optional: Override default model (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini